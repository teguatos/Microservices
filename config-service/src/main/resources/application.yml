server :
  port : 8091
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/teguatos/Microservices.git
          searchPaths: config-data
          username: teguatos
          password: 
          default-label: "main"
  security:
    user:
      name: root
      password: s3cr3t
